Here's where we want to get:
- Write SQL and pop it in SQL directory.
- Write reports header csv in its own directory.
- Set the cron schedule in another file.
- Test.
- Deploy!

Move all current SQLs into separate files in the sql directory.
Change all [..] constructs to [[..]].
